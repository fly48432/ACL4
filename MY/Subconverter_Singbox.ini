;dorian sing-box 订阅转换配置
;sing-box 的 outbounds 会被 subconverter 整体覆盖，所以分组+规则全部写在 BaseSingbox.json
;这里只声明 base 指向，并关闭规则生成器，避免 subconverter 改动 base

[common]
enable_rule_generator=false
overwrite_original_rules=false
singbox_rule_base=https://raw.githubusercontent.com/fly48432/ACL4/main/MY/BaseSingbox.json
